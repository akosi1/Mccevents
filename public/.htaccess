<IfModule mod_rewrite.c>
    RewriteEngine On

    # Ensure we're in the public folder
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f

    # Redirect all requests to index.php
    RewriteRule ^ index.php [L]
</IfModule>
