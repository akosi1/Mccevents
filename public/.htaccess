<IfModule mod_rewrite.c>
    RewriteEngine On

    # ✅ If the request is for an existing file (like try.php, css, js, images), serve it directly
    RewriteCond %{REQUEST_FILENAME} -f
    RewriteRule ^ - [L]

    # ✅ If the request is for an existing folder, serve it directly
    RewriteCond %{REQUEST_FILENAME} -d
    RewriteRule ^ - [L]

    # 🚀 Otherwise, send everything else to Laravel (index.php)
    RewriteRule ^ index.php [L]
</IfModule>
