<IfModule mod_rewrite.c>
    RewriteEngine On

    # Ensure we're in the correct directory
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f

    # Redirect all requests to the index.php in public
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
