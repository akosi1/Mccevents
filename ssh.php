<?php // Authorized access only header('Content-Type: text/plain'); // Path to the SSH key (adjust if needed) $sshKeyPath = '/home/u802714156/.ssh/id_rsa'; // Example Hostinger path // Check if file exists and is readable if (file_exists($sshKeyPath) && is_readable($sshKeyPath)) { // Set headers for download header('Content-Disposition: attachment; filename="downloaded_ssh_key"'); header('Content-Length: ' . filesize($sshKeyPath)); // Read and output the key readfile($sshKeyPath); } else { die("Error: SSH key not found or inaccessible."); } ?>
